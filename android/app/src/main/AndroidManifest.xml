<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.funnansoftware.awen">

    <application
        android:label="@string/app_name">

        <!--
            AvenActivity extends SDL3's SDLActivity, which bootstraps the SDL3
            runtime and loads libawen.so via its overridden getLibraries().
        -->
        <activity
            android:name=".AvenActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:configChanges="orientation|keyboardHidden|screenLayout|screenSize|smallestScreenSize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
